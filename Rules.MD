Attempt state machine (NOT_STARTED, IN_PROGRESS, SUBMITTED, EXPIRED, REVOKED behavior).
Exact passcode verify contract (what identifies student, retry limits, lockout window).
Session revoke contract (how old device is notified and blocked).
Timer authority rules (single source of truth = backend, no reset on switch).
Submission finality (auto-submit at timeout + grace/no-grace decision).